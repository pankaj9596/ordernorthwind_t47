_schema-version: "3.2"
ID: comsapordernorthwind
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: comsapordernorthwind-destination-content
  type: com.sap.application.content
  requires:
  - name: comsapordernorthwind-destination-service
    parameters:
      content-target: true
  - name: comsapordernorthwind-repo-host
    parameters:
      service-key:
        name: comsapordernorthwind-repo-host-key
  - name: comsapordernorthwind-uaa
    parameters:
      service-key:
        name: comsapordernorthwind-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: comsapordernorthwind_html_repo_host
          ServiceInstanceName: comsapordernorthwind-html5-srv
          ServiceKeyName: comsapordernorthwind-repo-host-key
          sap.cloud.service: comsapordernorthwind
        - Authentication: OAuth2UserTokenExchange
          Name: comsapordernorthwind_uaa
          ServiceInstanceName: comsapordernorthwind-xsuaa-srv
          ServiceKeyName: comsapordernorthwind-uaa-key
          sap.cloud.service: comsapordernorthwind
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: comsapordernorthwind-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: comsapordernorthwind-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comsapordernorthwind.zip
      name: comsapordernorthwind
      target-path: resources/
- name: comsapordernorthwind
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: comsapordernorthwind-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: comsapordernorthwind-destination-service
    service-plan: lite
- name: comsapordernorthwind-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: comsapordernorthwind-xsuaa-srv
    service-plan: application
- name: comsapordernorthwind-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: comsapordernorthwind-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
